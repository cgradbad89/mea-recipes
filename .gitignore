# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
.env.local

# Batch 4: large generated dry-run diffs (the reviewable reports are the batch4-*.md files)
scripts/canonical-dryrun-raw.json
scripts/canonical-dryrun-v1-raw.json
scripts/canonical-dryrun-v2-raw.json
scripts/reaudit-task1.json
.env*
# but do track the committed template (placeholder names/values only, no secrets)
!.env.example
